[project]
name = "ags"
description = "annotation guided serialization"
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
yaml = ["py-yaml12>=0.1.0"]

[tool.setuptools.dynamic]
version = {attr = "ags.__version__"}
